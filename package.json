{
  "name": "avma-ai-photo-editor",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "mkdir -p public && cp index.html manifest.json service-worker.js public/ && esbuild index.tsx --bundle --outfile=public/bundle.js --minify --sourcemap --loader:.tsx=tsx"
  },
  "dependencies": {
    "@google/genai": "^1.10.0",
    "react": "^19.1.0",
    "react-dom": "^19.1.0",
    "react-image-crop": "^11.0.6"
  },
  "devDependencies": {
    "esbuild": "^0.20.2"
  }
}
